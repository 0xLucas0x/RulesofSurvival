# --- Database ---
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# --- Auth / SIWE ---
NEXT_PUBLIC_DYNAMIC_ENV_ID="replace_with_dynamic_environment_id"
JWT_SECRET="replace_with_a_32_plus_char_secret"
# 32-byte raw string OR base64 that decodes to 32 bytes
CONFIG_ENCRYPTION_KEY="replace_with_32_byte_key_or_base64"
ADMIN_WALLET_ADDRESSES="0xadminwallet1,0xadminwallet2"
SIWE_DOMAIN="localhost:3000"
APP_BASE_URL="http://localhost:3000"

# --- Chain ---
MONAD_RPC_URL="https://testnet-rpc.monad.xyz"

# --- Optional fallback LLM key for Gemini path ---
API_KEY=""
